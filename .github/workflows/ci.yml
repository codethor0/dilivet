# DiliVet — ML-DSA diagnostics toolkit
# Copyright (c) 2025 Thor Thor (codethor0)
# Project: github.com/codethor0/dilivet
# LinkedIn: https://www.linkedin.com/in/thor-thor0

name: CI

on:
  push:
    branches:
      - main
      - 'feat/**'
  pull_request:
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/ci.yml'
      - 'fuzz/**'
      - 'code/**'
      - 'cmd/**'
      - 'web/**'
      - 'scripts/**'
      - 'tests/**'

# Cancel in-progress runs for the same PR/branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DISK_CAP_GB: 10
  MAX_LOG_MB: 5
  GO_VERSION: '1.23'

jobs:
  # Fast preflight: lint and types before full test suite
  preflight:
    name: preflight (lint + types)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - name: Check disk space
        shell: bash
        run: |
          if [ "${{ runner.os }}" != "Windows" ]; then
            df -h || echo "df command not available"
          fi
          echo "DISK_CAP_GB=${DISK_CAP_GB}" >> $GITHUB_ENV

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ env.GO_VERSION }}-${{ hashFiles('go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-${{ env.GO_VERSION }}-

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --out-format=github-actions --timeout=5m

      - name: go vet
        run: go vet ./...

      - name: Cleanup
        if: always()
        run: |
          if [ "${{ runner.os }}" != "Windows" ]; then
            du -sh ~/.cache/go-build ~/go/pkg/mod 2>/dev/null || true
          fi
          go clean -cache -modcache -testcache 2>/dev/null || true

  test:
    name: go 1.23 • ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: [preflight]
    if: always() && (needs.preflight.result == 'success' || needs.preflight.result == 'skipped' || github.event_name != 'pull_request')
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Check disk space
        shell: bash
        run: |
          if [ "${{ runner.os }}" != "Windows" ]; then
            df -h || echo "df command not available"
          fi
          echo "DISK_CAP_GB=${DISK_CAP_GB}" >> $GITHUB_ENV
          echo "MAX_LOG_MB=${MAX_LOG_MB}" >> $GITHUB_ENV

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Cache Go modules and build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-1.23-${{ hashFiles('go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-1.23-

      - name: go vet
        run: go vet ./...

      - name: go test (race)
        run: go test -race -p 4 ./...

      - name: golangci-lint
        if: matrix.os == 'ubuntu-latest'
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --out-format=github-actions --timeout=5m
        continue-on-error: true

      - name: Web backend tests
        if: matrix.os == 'ubuntu-latest'
        run: go test ./web/server/...
        continue-on-error: true

      # Frontend steps are optional - skip if they cause issues
      - name: Setup Node.js
        if: matrix.os == 'ubuntu-latest'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/ui/package.json
        continue-on-error: true

      - name: Install frontend dependencies
        if: matrix.os == 'ubuntu-latest'
        working-directory: web/ui
        run: npm ci || echo "Frontend dependencies failed, skipping frontend steps"
        continue-on-error: true

      - name: Frontend tests
        if: matrix.os == 'ubuntu-latest'
        working-directory: web/ui
        run: npm test -- --run || echo "Frontend tests failed, continuing..."
        continue-on-error: true

      - name: Frontend build
        if: matrix.os == 'ubuntu-latest'
        working-directory: web/ui
        run: npm run build || echo "Frontend build failed, continuing..."
        continue-on-error: true

      - name: Cleanup and report
        if: always()
        shell: bash
        run: |
          echo "=== Disk usage before cleanup ==="
          if [ "${{ runner.os }}" != "Windows" ]; then
            df -h || echo "df command not available"
            du -sh ~/.cache/go-build ~/go/pkg/mod 2>/dev/null || true
          else
            echo "Windows runner - skipping Unix-specific commands"
          fi
          go clean -cache -modcache -testcache 2>/dev/null || true
          echo "=== Disk usage after cleanup ==="
          if [ "${{ runner.os }}" != "Windows" ]; then
            df -h || echo "df command not available"
            echo "=== Total disk usage ==="
            df -h | tail -1 | awk '{print "Total: " $2 ", Used: " $3 ", Available: " $4}' || true
          else
            echo "Cleanup complete on Windows runner"
          fi
