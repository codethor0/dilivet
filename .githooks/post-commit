#!/bin/sh

if ! command -v git-lfs >/dev/null 2>&1; then
	cat >&2 <<'EOF'

warning: Git LFS is not installed; skipping git-lfs post-commit hook.
Install https://git-lfs.com/ if you intend to commit LFS-managed objects.

EOF
	exit 0
fi

git lfs post-commit "$@"

